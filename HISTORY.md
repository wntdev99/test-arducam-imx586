기술적인 논리 근거를 보강하여, **하드웨어(IMX586) - 커널(V4L2 드라이버) - 애플리케이션(OpenCV/Python)** 사이에서 발생하는 충돌과 해결 과정을 심층적으로 분석한 보고서입니다.

---

## 1. 문제의 본질: 커널-하드웨어 간 '상태 불일치'

Arducam IMX586과 같은 고해상도(48MP) USB3 카메라에서 **장치 잠금(Device Lock)**이 발생하는 근본 원인은 **데이터 스트리밍 중단 시의 비정상적인 시퀀스**에 있습니다.

* **V4L2 버퍼 메커니즘:** 카메라는 커널 메모리(mmap)에 데이터를 계속 밀어넣고, 앱은 이를 가져옵니다(`DQBUF`).
* **교착 상태(Deadlock):** 앱이 `SIGINT`로 갑자기 종료되면, 커널은 `STREAMOFF` ioctl을 통해 하드웨어에 중지 명령을 내려야 합니다. 하지만 IMX586 펌웨어가 특정 데이터 패킷을 처리 중일 때 이 명령을 받으면 응답하지 않고 **D-state(uninterruptible sleep)**에 빠지게 됩니다.
* **자원 고립:** 커널은 장치가 '사용 중'이라고 판단하여 파일 서술자(fd)를 해제하지 않고, 다음 실행 시 `Device or resource busy` 또는 `EIO(I/O Error)`를 반환합니다.

---

## 2. OpenCV(VideoCapture)의 구조적 한계와 실패 원인

OpenCV의 `cv2.VideoCapture`는 범용성을 위해 V4L2의 세부 제어권을 감추고 있습니다(Abstraction). 이것이 문제 해결의 걸림돌이 되었습니다.

| 시도된 기술적 접근 | 논리적 근거 | 실패 원인 (Why it failed) |
| --- | --- | --- |
| **`pthread_sigmask`** | `cap.read()`라는 블로킹 ioctl 구간에서 시그널 발생 시 시스템 콜이 오염되는 것을 방지. | 시그널은 막았으나, `cap.release()` 시점에서 발생하는 드라이버 레벨의 정지 지연은 제어 불가. |
| **`USBDEVFS_RESET`** | 소프트웨어 cleanup이 실패했을 때 USB 버스 레벨에서 하드웨어를 강제 재부팅. | OpenCV가 장치 fd를 쥐고 있는 상태에서 리셋하면, 커널 드라이버와 OpenCV 내부 상태가 충돌하여 커널 패닉이나 좀비 프로세스 생성. |
| **`dup2(/dev/null, fd)`** | OpenCV가 점유한 fd를 `/dev/null`로 바꿔치기해 강제로 연결을 끊으려 함. | OpenCV 내부의 C++ 객체는 여전히 유효한 fd라고 믿고 ioctl을 시도하여 `Bad file descriptor` 및 세그멘테이션 오류 유발. |

---

## 3. 해결책: Raw V4L2 API 기반의 상태 머신 설계

OpenCV를 제거하고 **V4L2 ioctl을 직접 호출**함으로써, 자원 해제의 순서와 예외 처리를 완전히 통제(Deterministic Control)할 수 있게 되었습니다.

### 핵심 로직: 원자적 자원 해제 (Atomic Cleanup)

V4L2Camera 클래스는 다음과 같은 엄격한 순서로 자원을 해제합니다.

1. **`VIDIOC_STREAMOFF` (비동기 처리):** 가장 위험한 구간입니다. 이를 데몬 스레드에서 실행하고 **3초 타임아웃**을 적용했습니다. 하드웨어가 응답하지 않더라도 다음 단계로 강제 진행합니다.
2. **`munmap`:** 커널 메모리 매핑을 해제하여 메모리 누수를 방지합니다.
3. **`VIDIOC_REQBUFS(0)`:** 하드웨어에 할당된 모든 버퍼를 명시적으로 반납합니다.
4. **`close(fd)`:** 커널 드라이버에 장치 사용 종료를 알립니다. 이 단계가 완료되어야 시스템 레벨에서 장치 잠금이 풀립니다.
5. **`USB_RESET`:** 마지막으로 USB 장치를 초기 상태로 되돌려 다음 `run`에서 깨끗한 상태로 시작하게 합니다.

---

## 4. 최종 설계의 기술적 방어선 (Defensive Programming)

* **D-state 방어:** `os._exit(1)`은 일반적인 상황에선 위험하지만, 드라이버가 D-state에 빠져 프로세스가 죽지 않을 때는 유일한 탈출구입니다. 이를 `join(timeout)`과 결합해 시스템 전체의 Hang을 방지했습니다.
* **유형 캐스팅 보정:** Python의 동적 타이핑으로 인해 `config.json`에서 읽어온 `3.0`(float)이 C-struct인 V4L2 프레임 레이트 설정(`int`)에 전달되어 발생하는 `TypeError`를 명시적 캐스팅(`int()`)으로 해결했습니다.
* **Signal Inheritance:** Bash에서 `&`로 실행할 때 `SIGINT`가 무시되는 특성을 `signal.signal(signal.SIGINT, signal.SIG_DFL)`로 복원하여, 어떤 실행 환경에서도 종료 신호가 유실되지 않도록 보장했습니다.

---

## 5. 결론 및 향후 검증

현재의 설계는 **"소프트웨어(OpenCV)가 감추고 있던 커널 제어권을 다시 가져와, 하드웨어의 오작동 시나리오를 소프트웨어가 타임아웃으로 강제 종료"**하는 구조입니다.

**다음 단계:**

* `stress.sh`를 통해 `STREAMOFF` 타임아웃이 실제로 D-state 상황에서 프로세스를 안전하게 종료시키고 다음 테스트(Run)를 성공시키는지 데이터로 입증해야 합니다.
* **수행할 명령:** `taskset -c 0-3 ./stress.sh safe` (CPU 경합 상황까지 시뮬레이션)

이 구조라면 하드웨어 펌웨어의 한계를 소프트웨어 로직으로 극복하여 100+ PASS 달성이 논리적으로 가능합니다. 추가로 특정 `ioctl`의 상세 파라미터나 로그 분석이 필요하신가요?